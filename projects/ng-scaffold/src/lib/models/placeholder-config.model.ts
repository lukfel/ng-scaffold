import { Button } from './menu-button.model';

export interface PlaceholderConfig {
  matIcon?: string;
  svgIcon?: string;
  heading?: string;
  message?: string;
  button?: Button;
  cssClass?: string;
}
