import { Button } from './menu-button.model';

export interface PlaceholderConfig {
  matIcon?: string;
  svgIcon?: string;
  title?: string;
  message?: string;
  button?: Button;
  cssClass?: string;
}
